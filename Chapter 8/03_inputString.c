#include <stdio.h>

int main(){
    char Ch[5];
    printf("Enter the Stirng\n");
    scanf("%s",Ch);
    printf("The String is %s", Ch);
    // This is only used when the stirng does not have multi word characters
    return 0;
}